import { Injectable } from '@angular/core';

export interface listOfFilms {

    genres: [],
    title: string,
    id: number,
    overview: string,
    runtime: number,
    vote_count: number,
    poster_path: string,
    release_date: string
   
}
